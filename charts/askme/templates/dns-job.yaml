{{- if .Values.dns.ovh.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "askme.fullname" . }}-dns-setup
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "askme.labels" . | nindent 4 }}
    app.kubernetes.io/component: dns-manager
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "1" 
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        {{- include "askme.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: dns-manager
    spec:
      restartPolicy: Never
      containers:
      - name: ovh-dns-manager
        image: python:3.11-slim
        command:
        - /bin/bash
        - -c
        - |
          set -e
          echo "🌐 Installation des dépendances OVH API..."
          pip install --no-cache-dir requests
          
          echo "📋 Configuration DNS pour client: {{ .Release.Name }}"
          echo "🎯 Domaine cible: {{ .Values.client.domain }}"
          echo "🏷️ Zone DNS: {{ .Values.dns.ovh.zone }}"
          
          # Variables
          CLIENT_SUBDOMAIN=$(echo "{{ .Values.client.domain }}" | sed 's/\.{{ .Values.dns.ovh.zone }}$//')
          TARGET_IP="{{ .Values.dns.ovh.targetIP }}"
          ZONE="{{ .Values.dns.ovh.zone }}"
          TTL="{{ .Values.dns.ovh.ttl }}"
          
          echo "📝 Sous-domaine: $CLIENT_SUBDOMAIN"
          
          # Auto-détection IP si nécessaire
          if [ "$TARGET_IP" = "auto" ] || [ -z "$TARGET_IP" ]; then
            echo "🔄 Recherche automatique de l'IP d'ingress..."
            for i in {1..30}; do
              # Essayer d'abord loadBalancer.ingress
              TARGET_IP=$(kubectl get ingress {{ include "askme.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
              
              # Si pas trouvé, chercher dans les annotations (Rancher/OVH)
              if [ -z "$TARGET_IP" ] || [ "$TARGET_IP" = "null" ]; then
                TARGET_IP=$(kubectl get ingress {{ include "askme.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath='{.metadata.annotations.field\.cattle\.io/publicEndpoints}' 2>/dev/null | grep -o '"addresses":\["[^"]*"' | sed 's/.*"\([^"]*\)"/\1/' | head -1 || echo "")
              fi
              
              if [ -n "$TARGET_IP" ] && [ "$TARGET_IP" != "null" ]; then
                echo "✅ IP trouvée automatiquement: $TARGET_IP"
                break
              fi
              echo "⏳ Attente IP ingress... ($i/30)"
              sleep 10
            done
            
            if [ -z "$TARGET_IP" ] || [ "$TARGET_IP" = "null" ]; then
              echo "❌ Impossible de récupérer l'IP d'ingress automatiquement"
              echo "💡 Définissez dns.ovh.targetIP manuellement dans values.yaml"
              exit 1
            fi
          fi
          
          echo "🎯 IP cible finale: $TARGET_IP"
          
          # Script Python OVH API REEL
          python3 << 'PYTHON_EOF'
          import requests
          import json
          import hashlib
          import time
          import os
          import sys
          
          # Configuration OVH API depuis les variables d'environnement
          OVH_APP_KEY = os.environ['OVH_APP_KEY']
          OVH_APP_SECRET = os.environ['OVH_APP_SECRET'] 
          OVH_CONSUMER_KEY = os.environ['OVH_CONSUMER_KEY']
          OVH_ENDPOINT = os.environ['OVH_ENDPOINT']
          
          # Variables du DNS
          zone = os.environ['DNS_ZONE']
          subdomain = os.environ['DNS_SUBDOMAIN']
          target_ip = os.environ['DNS_TARGET_IP']
          ttl = int(os.environ['DNS_TTL'])
          
          # URLs API OVH selon l'endpoint
          api_endpoints = {
              'ovh-eu': 'https://eu.api.ovh.com/1.0',
              'ovh-ca': 'https://ca.api.ovh.com/1.0', 
              'ovh-us': 'https://api.us.ovhcloud.com/1.0'
          }
          
          base_url = api_endpoints.get(OVH_ENDPOINT, api_endpoints['ovh-eu'])
          
          def ovh_api_signature(method, query, body, timestamp):
              """Génère la signature pour l'API OVH"""
              s = '+'.join([
                  OVH_APP_SECRET,
                  OVH_CONSUMER_KEY, 
                  method.upper(),
                  query,
                  body,
                  str(timestamp)
              ])
              return '$1$' + hashlib.sha1(s.encode()).hexdigest()
          
          def ovh_api_call(method, path, data=None):
              """Effectue un appel à l'API OVH"""
              query = base_url + path
              body = json.dumps(data) if data else ''
              timestamp = int(time.time())
              
              headers = {
                  'X-Ovh-Application': OVH_APP_KEY,
                  'X-Ovh-Consumer': OVH_CONSUMER_KEY,
                  'X-Ovh-Signature': ovh_api_signature(method, query, body, timestamp),
                  'X-Ovh-Timestamp': str(timestamp),
                  'Content-Type': 'application/json'
              }
              
              response = requests.request(method, query, headers=headers, data=body)
              
              if not response.ok:
                  print(f"❌ Erreur API OVH: {response.status_code} - {response.text}")
                  raise Exception(f"API Error: {response.status_code}")
              
              return response.json() if response.text else {}
          
          def main():
              print(f"🌐 Création enregistrement DNS: {subdomain}.{zone} -> {target_ip}")
              print(f"📍 API OVH: {base_url}")
              print(f"⏱️ TTL: {ttl}s")
              
              try:
                  # 1. Vérifier si l'enregistrement existe déjà
                  print("🔍 Vérification des enregistrements existants...")
                  existing_records = ovh_api_call('GET', f'/domain/zone/{zone}/record?fieldType=A&subDomain={subdomain}')
                  
                  # 2. Supprimer les anciens enregistrements si ils existent
                  for record_id in existing_records:
                      record_info = ovh_api_call('GET', f'/domain/zone/{zone}/record/{record_id}')
                      print(f"🗑️ Suppression ancien enregistrement: {record_info.get('subDomain', '')}.{zone} -> {record_info.get('target', '')}")
                      ovh_api_call('DELETE', f'/domain/zone/{zone}/record/{record_id}')
                  
                  # 3. Créer le nouvel enregistrement
                  record_data = {
                      'fieldType': 'A',
                      'subDomain': subdomain,
                      'target': target_ip,
                      'ttl': ttl
                  }
                  
                  print(f"➕ Création nouvel enregistrement DNS...")
                  new_record = ovh_api_call('POST', f'/domain/zone/{zone}/record', record_data)
                  print(f"✅ Enregistrement créé avec l'ID: {new_record.get('id', 'N/A')}")
                  
                  # 4. Rafraîchir la zone DNS
                  print("🔄 Rafraîchissement de la zone DNS...")
                  ovh_api_call('POST', f'/domain/zone/{zone}/refresh')
                  
                  print(f"🎉 DNS configuré avec succès!")
                  print(f"📋 Résultat: {subdomain}.{zone} -> {target_ip} (TTL: {ttl}s)")
                  
              except Exception as e:
                  print(f"❌ Erreur lors de la configuration DNS: {str(e)}")
                  sys.exit(1)
          
          if __name__ == "__main__":
              main()
          PYTHON_EOF
          
          echo "✅ Configuration DNS terminée avec succès!"
        
        env:
        - name: OVH_APP_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "askme.fullname" . }}-secrets
              key: OVH_APP_KEY
        - name: OVH_APP_SECRET
          valueFrom:
            secretKeyRef:
              name: {{ include "askme.fullname" . }}-secrets
              key: OVH_APP_SECRET
        - name: OVH_CONSUMER_KEY
          valueFrom:
            secretKeyRef:
              name: {{ include "askme.fullname" . }}-secrets
              key: OVH_CONSUMER_KEY
        - name: OVH_ENDPOINT
          value: {{ .Values.dns.ovh.endpoint | quote }}
        - name: DNS_ZONE
          value: {{ .Values.dns.ovh.zone | quote }}
        - name: DNS_SUBDOMAIN
          value: "{{ .Values.client.domain | replace (printf ".%s" .Values.dns.ovh.zone) "" }}"
        - name: DNS_TARGET_IP
          value: {{ .Values.dns.ovh.targetIP | quote }}
        - name: DNS_TTL
          value: {{ .Values.dns.ovh.ttl | quote }}
        
        resources:
          requests:
            memory: "128Mi"
            cpu: "200m"
          limits:
            memory: "256Mi"
            cpu: "500m"
{{- end }}